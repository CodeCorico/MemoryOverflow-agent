{
  "name": "memoryoverflow-publish",
  "description": "Automated website publication system",
  "version": "0.0.1",
  "main": "publish.js",
  "scripts": {
    "start": "node publish.js",
    "test": "mocha --reporter spec --bail --check-leaks",
    "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --reporter spec --check-leaks"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/CodeCorico/MemoryOverflow-publish.git"
  },
  "dependencies": {
    "extend": "^2.0.0",
    "fs-extra": "^0.12.0",
    "worker-farm": "^1.0.1"
  },
  "devDependencies": {
    "mocha": "^1.21.4",
    "chai": "^1.9.2",
    "superagent": "^0.20.0",
    "istanbul": "^0.3.2"
  },
  "licenses": [{
    "type": "MIT",
    "url": "https://github.com/CodeCorico/MemoryOverflow-publish.git"
  }]
}
